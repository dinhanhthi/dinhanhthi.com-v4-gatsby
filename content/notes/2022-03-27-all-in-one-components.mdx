---
title: "All-in-One Components"
tags: [mlops, ml, thu nghiem]
---

import { LoremIpsum } from 'react-lorem-ipsum';

<LoremIpsum p={2} />

## Math equations

Try inline math equation $\dfrac{\sqrt{2}}{x+y}$ and then with block,

$$
\underbrace{SDR(S,O)}_{\text{Standard Deviation Reduction}}= \underbrace{SD(S)}_{\text{SD before split}}- \underbrace{\sum_j P(O_j | S) \times SD(S,O_j)}_{\text{weighted SD after split}}
$$

## Images

```
![Alt text goes here](./img/home.jpg)
```

![Alt that will be used as caption.](./img/home.jpg)

With caption?

```
![Alt that will be used as caption.](./img/home.jpg)
_This is a caption with **bold text** and `code`._
```

![Alt that will be used as caption.](./img/home.jpg)
_This is a caption with **bold text** and `code`._

Custom width?

<img src="./img/home.jpg" alt="My home" width="200px"/>

## H2: Text with `code`

Thu nghiem **bold text** or *italic text* hay ***bold and italic same time*** <u>underline text</u> nay no xem duoc khong.

### H3: Inline code `code`

Thu nghiem `inline-code` xem the nao.

#### H4: Order `code`

- Unorder 1
- Unorder 2
  - Unorder 2.1
  - Unorder 2.2
    - Unorder 2.2.1
- Unorder 3

1. Order 1
2. Order 2
   1. Order 2.1
   2. Order 2.2
      1. Order 2.2.1
3. Order 3

### Code block

Simple (not that, highlight a single line with a comment <code>// highlight-line</code> at the line you wanna highlight),

````
```python
a = (1, 2, 3) # tuple
x = list(a) # highlight-line
```
````

```python
a = (1, 2, 3) # tuple
x = list(a) # highlight-line
```

With numbering,

```javascript{numberLines: true}
// In your gatsby-config.js
plugins: [
  {
    resolve: `gatsby-transformer-remark`,
    options: {
      plugins: [
        `gatsby-remark-prismjs`,
      ]
    }
  }
]
```

A long script,

```javascript{numberLines: true}
// In your gatsby-config.js
plugins: [
  {
    resolve: `gatsby-transformer-remark`,
    options: {
      plugins: [
        `gatsby-remark-prismjs`, // highlight-line
      ]
    }
  }
]

// In your gatsby-config.js
plugins: [
  {
    resolve: `gatsby-transformer-remark`,
    options: {
      plugins: [
        `gatsby-remark-prismjs`,
      ]
    }
  }
]

// In your gatsby-config.js
plugins: [
  {
    resolve: `gatsby-transformer-remark`,
    options: {
      plugins: [
        `gatsby-remark-prismjs`,
      ]
    }
  }
]
```

Numbering starts at 5 and there is a very long line,

```javascript{numberLines: 5}
// In your gatsby-config.js
plugins: [
  {
    // Try with a very long line, with a very long line, with a very long line, with a very long line,
    resolve: `gatsby-transformer-remark`,
    options: {
      plugins: [
        `gatsby-remark-prismjs`,
      ]
    }
  }
]
```

With highlighted lines,

```javascript{1,4-6}
plugins: [
  {
    resolve: `gatsby-transformer-remark`,
    options: {
      plugins: [
        `gatsby-remark-prismjs`,
      ]
    }
  }
]
```

Combine highlighted lines and numbering,

```javascript{1,4-6}{numberLines: true}
plugins: [
  {
    resolve: `gatsby-transformer-remark`,
    options: {
      plugins: [
        `gatsby-remark-prismjs`,
      ]
    }
  }
]
```

With title?

```js:title=example-file.js
alert('how cool is this!');
```

```js:title=example-file.js {1,4-6}{numberLines: true}
plugins: [
  {
    resolve: `gatsby-transformer-remark`,
    options: {
      plugins: [
        `gatsby-remark-prismjs`,
      ]
    }
  }
]
```